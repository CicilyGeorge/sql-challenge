departments
-----
dept_no VARCHAR(5) PK
dept_name VARCHAR(50)

titles
-----
title_id VARCHAR(5) PK
title VARCHAR(50)

employees
-----
emp_no BIGINT PK
emp_title_id VARCHAR(5) FK >- "titles"."title_id"
birth_date DATE
first_name VARCHAR(50)
last_name VARCHAR(50)
sex CHAR(1)
hire_date DATE 

salaries
-----
emp_no BIGINT PK FK
salary BIGINT

dept_manager
-----
dept_no VARCHAR(5) PK FK
emp_no BIGINT PK FK

dept_emp
-----
emp_no BIGINT PK FK
dept_no VARCHAR(5) PK FK